# ---------------------------------------------------------------------------
# Reusable test template
# Usage:
#   test:
#     extends: .test
#     variables:
#       WORKING_DIRECTORY: "services/my-api"
# ---------------------------------------------------------------------------

.test:
  stage: test
  image: python:${PYTHON_VERSION}
  before_script:
    - cd services/inference-api && pip install --quiet -r requirements.txt
  script:
    - pytest tests/ -v --junit-xml=report.xml
  artifacts:
    when: always
    reports:
      junit: "services/inference-api/report.xml"
    expire_in: 30 days
