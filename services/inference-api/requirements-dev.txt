setuptools>=70.0.0
fastapi==0.115.0
uvicorn[standard]==0.30.6
redis==5.0.8
prometheus-client==0.20.0

opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-http==1.27.0

pydantic>=2.7.0
httpx>=0.27.0
pytest>=8.2.0

# transformers without torch — tests mock transformers.pipeline so torch is
# never needed locally. torch==2.10.0+cpu is Linux-only (Docker/CI only).
transformers==4.44.0
